#!/bin/bash

# scriptName: print_terminal_history

# Objective: prints the entire history of the terminal, aka history and writes it to a file
# Inputs: None
# Outputs: Writes history to a file

history > terminal_history.txt